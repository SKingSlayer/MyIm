<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace绑定一个dao接口-->
<mapper namespace="com.example.demo.MyData.Dao.HistoryDao">

    <insert id="setHistory" parameterType="com.example.demo.MyData.Entity.CharHistory">
        INSERT INTO CharHistory(send,recv,myTime,history) VALUES (#{send},#{recv},#{myTime},#{history})
    </insert>

</mapper>